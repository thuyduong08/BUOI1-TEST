const diem_so =4;
    if (diem_so >= 9) {
        console.log("loai A");
    }
    else if (diem_so >=7&& diem_so <9 )
        console.log("loai B");
    else if (diem_so >=5 && diem_so <7)
        console.log("loai C");
    else{
        console.log ("loai F");
    }